import React from 'react';

const Header = () => {
    return (
        <div className="text-center header text-primary" style={{background:"url(Images/header.svg)"}}>
            <h2 className="p-3 text-white">Get Latest Movie Detail and Book Your ticket</h2>
        </div>
    );
};

export default Header;